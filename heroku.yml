build:
  docker:
    worker: Dockerfile
    command:
      pip install virtualenv --user
      pip install -r requirements.txt --user
run:
  worker:
    command:
      - python3 lichess-bot.py -u
    image: worker
